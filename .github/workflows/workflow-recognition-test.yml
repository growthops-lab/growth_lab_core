name: Workflow Recognition Test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  recognition-test:
    name: Workflow Recognition Test
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Confirm workflow execution
        run: echo "GitHub Actions workflow recognition test passed."
