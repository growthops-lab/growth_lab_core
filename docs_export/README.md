# Docs Export

This folder is used for exported human-readable documents.

Examples:

- Word documents
- PDF documents
- Review copies
- Approved versions

Markdown files in architecture/master are the primary source.
Exported files are secondary artifacts.
